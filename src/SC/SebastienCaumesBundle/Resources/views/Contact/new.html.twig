{% extends '::base.html.twig' %}
{% from 'SCSebastienCaumesBundle:macros:macros.html.twig' import back %}
{% block body %}

    {% for flashMessage in app.session.flashbag.get('sendmail') %}
        <div class="flash-notice center-block text-info">
            {{ flashMessage }}
        </div>
    {% endfor %}

    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="text-center"><h2>Contactez-moi</h2></div>
            {{ back('sc_sebastien_caumes_homepage') }}
            {{ form(form) }}
        </div>
    </div>


{% endblock %}